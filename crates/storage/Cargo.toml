[package]
name = "chronicle-storage"
version = "0.1.0"
edition = "2024"

[dependencies]
bytes = "1"
crc32fast = "1"
thiserror = "2"
tracing = "0.1"

[dev-dependencies]
criterion = { version = "0.8.2", features = ["html_reports"] }
tempfile = "3"

[[bench]]
name = "storage"
harness = false
